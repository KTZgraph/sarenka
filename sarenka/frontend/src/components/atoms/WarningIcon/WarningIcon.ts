import styled from 'styled-components';
import { ReactComponent as Icon } from 'static/warningIcon.svg';

const WarningIcon = styled(Icon)`
  margin-left: 10px;
  transform: translateY(25%);
`;

export default WarningIcon;
